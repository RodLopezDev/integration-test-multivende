interface Integration {
  id: string;
  name: string;
  clientId: string;
  clientSecret: string;
  clientCode: string;
  clientToken: string;
  clientRefreshToken: string;
}

export default Integration;
